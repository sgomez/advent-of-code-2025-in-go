## Advent of Code 2025 (Go)

### Run the solutions
```bash
go run ./main.go [--sample] <DAY>
```

- `<DAY>`: day to run between 01 and 12.
- `--sample`: optional flag to load `dayNN-sample.txt` instead of the default `dayNN.txt`.

### Run all tests
```bash
go test ./...
```
